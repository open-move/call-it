node_modules
.DS_Store
dist
dist-ssr
*.local
.env*
# Prod build vars are public (VITE_* URLs + Dynamic env id); commit so CI builds have them.
!.env.production
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# typescript
*.tsbuildinfo
