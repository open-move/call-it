services:
  keeper:
    build: .
    command: ["watch"]
    env_file: .env
    restart: unless-stopped
    volumes:
      - keeper-data:/app/data
    ports:
      - 4000:4000

volumes:
  keeper-data:
